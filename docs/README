November 30, 2021


DIG-default-executor.md
	Notes on the default thread executor (all tasks run in main server thread).

DIG-jdk-logging-levels.txt
	Notes on how much core Java code uses SystemLogger (not much).

DIG-server-backlog-parameter.md
	Notes on TCP socket backlog setting (max pending connections = 50).

DIG-server-configuration.md
	Environmental variables that control Sun's HTTP server that comes with Java.

HOWTO-install-wrk-on-OSX.txt
	Notes on using wrk loader (ended up sticking with ab).

HOWTO-intellij-and-module-source-path.txt
	How to configure IntelliJ to work with Java 9 modules.

HOWTO-prettier-javafmt.txt
	How to install prettier Java formatter.

footprint-analysis-part1.txt
footprint-analysis-part2.txt
	Memory footprint notes.

memtest_UseSerialGC_3m.txt
	ApacheBench and tinyjhttpd memory usage statistics for memory test.
